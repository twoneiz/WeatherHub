// Copy this file to `config.js` and fill in your own keys. Keep `config.js` out of version control.
window.CONFIG = {
  // Google Maps JavaScript API key (required for map rendering)
  googleMapsApiKey: "",
  // OpenWeather API key (required for weather data)
  openWeatherApiKey: "",
  // Google Cloud Translation API key (optional, only needed for language translations)
  googleTranslateApiKey: ""
};
